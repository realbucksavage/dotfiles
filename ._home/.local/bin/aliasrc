#!/bin/bash

alias rr='source /etc/profile && source ~/.bashrc'
alias e='exit'
alias c='clear'
alias i3c='vim ~/.config/i3/config'
alias i3b='vim ~/.config/i3blocks/config'

alias sp='sudo pacman'

function mcd() {
  mkdir -p $1
  cd $1
}
